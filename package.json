{
  "name": "handyman-services",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "mkdir -p ./dist && npx tailwindcss -i ./styles.css -o ./dist/output.css --minify && cp -r ./images ./dist/",
    "start": "mkdir -p ./dist && npx tailwindcss -i ./styles.css -o ./dist/output.css --watch && cp -r ./images ./dist/"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "autoprefixer": "^10.4.20",
    "postcss": "^8.4.49",
    "tailwindcss": "^3.4.17"
  }
}
